import { Button } from "@/components/ui/button";
import { Dialog, DialogContent, DialogDescription, DialogFooter, DialogHeader, DialogTitle } from "@/components/ui/dialog";

export default function PriceReviewDialog({ review, canReview, onClose, onDecision, processing }) {
  const hasPriceChange = review && Math.abs(review.catalog_price - review.retailer_price) >= 0.01;
  return <Dialog open={Boolean(review)} onOpenChange={open => !open && onClose()}><DialogContent><DialogHeader><DialogTitle>{hasPriceChange ? "Price discrepancy review" : "Product availability review"}</DialogTitle><DialogDescription>Review the latest information found on the linked retailer page.</DialogDescription></DialogHeader>{review && <div className="space-y-3 text-sm"><p><b>Item:</b> {review.product_name}</p><div className="grid grid-cols-2 gap-3"><div className="rounded-lg bg-slate-100 p-3"><span className="block text-slate-500">Catalog</span><b>${review.catalog_price.toFixed(2)}</b></div><div className="rounded-lg bg-amber-50 p-3"><span className="block text-slate-500">{review.retailer_name}</span><b>${review.retailer_price.toFixed(2)}</b></div></div><p><b>Availability:</b> {review.availability || "Not listed"}</p><a className="text-indigo-600 underline" href={review.source_url} target="_blank" rel="noreferrer">Open retailer listing</a></div>}<DialogFooter>{canReview ? <><Button variant="outline" onClick={() => onDecision("dismiss")} disabled={processing}>{hasPriceChange ? "Keep catalog price" : "Dismiss alert"}</Button><Button onClick={() => onDecision("confirm")} disabled={processing}>{processing ? "Saving…" : hasPriceChange ? "Confirm retailer price" : "Confirm review"}</Button></> : <Button onClick={onClose}>Close</Button>}</DialogFooter></DialogContent></Dialog>;
}